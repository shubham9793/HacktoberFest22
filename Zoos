#include<stdio.h>
#include<string.h>
int main()
{
    char a[20];
    int i,x=0,y=0;
    scanf("%s",a);
    for(i=0;a[i]!='\0';i++)
    {
        if(a[i]=='z')
            x++;
        else
            y++;
    }    
    if(y==2*x)
        printf("Yes");
    else
        printf("No");
}
